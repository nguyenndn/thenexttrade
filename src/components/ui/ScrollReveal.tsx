/**
 * ScrollReveal is now an alias for FadeIn (lightweight CSS + IntersectionObserver).
 * Previously used framer-motion — replaced for performance.
 * API is backward-compatible: children, className, delay, direction.
 */
export { FadeIn as ScrollReveal } from "./FadeIn";
