/**
 * Telegram channel configuration.
 * Single source of truth for the public GoldScalperNinja channel invite link.
 *
 * NOTE: this is a PRIVATE channel invite link (`t.me/+hash`), so the channel
 * has no public `@handle` — users join via this link only. Join counts are
 * reported by Telegram itself via the invite link's member stats.
 */
export const TELEGRAM_CHANNEL_URL =
    "https://t.me/+zS-_Gmt8SCQxYzA1";
