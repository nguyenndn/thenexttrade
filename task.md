# Task List: Edge Missions Guided UX

- [x] **Phase 1: Extend Mission Config**
  - Update `src/config/edge-missions.ts` with new fields (`cadence`, `ctaLabel`, `ctaHref`, `whyItMatters`, `priority`).
- [x] **Phase 2: Add Weekly Period Support**
  - Update `prisma/schema.prisma` (`UserMissionProgress.periodKey`).
  - Add period logic to `src/lib/services/edge-missions.service.ts`.
- [x] **Phase 3: Make Weekly Progress Truly Weekly**
  - Update progress queries in `edge-missions.service.ts` to filter by week.
  - Update claim logic with `periodKey`.
- [x] **Phase 4: Record Real Product Events**
  - Update `src/lib/edge-awards.ts` with `recordEdgeEventOnce`.
  - Update `src/app/api/lessons/[id]/complete/route.ts` to record event.
  - Update `src/app/api/quizzes/[id]/submit/route.ts` to record event.
- [x] **Phase 5: Add Next Best Action**
  - Create `NextBestActionCard.tsx`.
  - Update `MissionsClient.tsx` with `getNextBestMission` logic.
- [x] **Phase 6: Add CTA To Mission Cards**
  - Update `MissionCard.tsx` with CTAs and `whyItMatters`.
- [x] **Phase 7: Improve Page Grouping**
  - Restructure `MissionsClient.tsx` layout.
- [x] **Phase 8: Redirect Fix**
  - Update `src/app/dashboard/missions/page.tsx` redirect from `/login` to `/auth/login`.
- [x] **Phase 9: Verification**
  - Run `npx tsc --noEmit`.
  - Run `npx next build`.
  - Self-review.
