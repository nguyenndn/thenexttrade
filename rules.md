# Workspace Rules & Conventions

> File này được các workflow Antigravity (`/dev`, `/plan_feature`, `/code_review`) khai báo là
> "bắt buộc đọc trước khi code". Toàn bộ quy tắc đã được gộp vào **`AGENTS.md`** (cùng thư mục).

## → Đọc và tuân thủ `AGENTS.md` — Standing Execution Contract (7 rules + Design System + Tech Conventions).

Nếu có xung đột giữa các quy tắc, thứ tự ưu tiên:
`AGENTS.md` (execution contract) → plan / `implementation_plan.md` → conventions → `design/ui-guide.md`.
